require "bundler/gem_tasks"

require 'rake/extensiontask'

Rake::ExtensionTask.new "s_matrix" do |ext|
  ext.ext_dir = 'ext/s_matrix'
  ext.lib_dir = "lib/s_matrix"
end

#Rake::ExtensionTask.new('s_matrix')

